[Unit]
Description=Bring up the CAN bus interface
After=network.target

[Service]
User=root
Group=root
ExecStart=/usr/local/bin/can-iface

[Install]
WantedBy=busmaster.target
WantedBy=can-socat-setup.target
